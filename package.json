{
  "name": "renewly-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers backend fronting Claude for Renewly AI Assist (free tier).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.9.2",
    "wrangler": "^3.78.0"
  }
}
